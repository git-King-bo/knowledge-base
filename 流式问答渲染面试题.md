# 流式问答渲染：项目面试题与两轮深度追问

适用方向：Vue / React 前端、AI 应用前端、全栈知识库项目。整理日期：2026-09-08。

本文围绕本项目的 fetch + SSE、Markdown 渲染、停止与继续生成展开，共 12 个主题、36 个问答。每个主题按“主问题 → 第一轮追问 → 第二轮追问”递进。标为“可演进”的内容是设计建议，不代表项目已经实现。用户所说的“xxr”在本场景中按 XSS（跨站脚本攻击）处理。

## 项目介绍：面试开场可以怎么说

我在知识库问答中实现了一条完整的流式链路：后端接收模型增量，转成统一的 SSE 事件；前端通过支持自定义请求头的 fetch 消费流，分离字节解码、事件解析和界面渲染。文本先进入非响应式缓冲区，再通过 requestAnimationFrame 每帧最多执行一次完整 Markdown 快照解析和 HTML 清洗。停止时保留已经收到的内容，继续时携带原问题、资料快照和已有回答发起续写，并保留来源引用和真实 Token 统计。

这里的关键不只是让文字逐字出现，而是处理分包、渲染成本、半成品 Markdown、安全边界和请求生命周期。

### 实现定位

| 环节 | 项目文件 | 核心职责 |
| --- | --- | --- |
| 请求层 | [api.ts](frontend/src/lib/api.ts) | 自定义请求头、fetch、事件校验、续写参数 |
| 协议层 | [sse.ts](frontend/src/lib/sse.ts) | UTF-8 解码、SSE 分行与事件组装、取消读取 |
| 调度层 | [frameBuffer.ts](frontend/src/lib/frameBuffer.ts) | 每帧合并、结束刷新、卸载清理 |
| 渲染层 | [renderMarkdown.ts](frontend/src/lib/renderMarkdown.ts) | Markdown、引用按钮、链接策略、DOMPurify |
| 交互层 | [RetrievalWorkspace.vue](frontend/src/views/RetrievalWorkspace.vue) | 状态、停止续写、滚动、引用定位 |
| 服务层 | [knowledge.py](backend/app/api/routes/knowledge.py) | 检索、上下文、SSE 事件、日志 |
| 模型层 | [openai_compatible.py](backend/app/ai/providers/openai_compatible.py) | 上游流读取、增量和 usage 提取 |

## 01. 为什么使用 fetch 接收 SSE，而不是直接用 EventSource？

### 主问题：你的流式问答为什么选择 fetch + SSE？

**参考回答：**

问答需要提交问题、模型、知识库和续写上下文，适合使用 POST 请求体；还需要自定义 Authorization 等请求头。原生 EventSource 的构造接口不提供任意请求头和 POST 请求体配置，因此项目使用 fetch 获取响应流，再自行解析 SSE。

SSE 是事件格式，EventSource 是浏览器消费这种格式的 API，两者不能混为一谈。业务主要是服务端向客户端连续推送回答，使用 HTTP 流就能满足需求，不必为了流式效果引入双向 WebSocket。

### 第一轮追问：用了 fetch，是不是浏览器会自动帮你处理 SSE 重连？

**参考回答：**

不会。fetch 给我的是响应体字节流，我需要处理解码、事件分隔、错误和取消。项目也没有实现自动重连。

生成类 POST 请求不能像普通查询那样无条件重试：服务端可能已经调用模型，只是结果没有送达浏览器。盲目重试可能产生重复回答和重复费用。当前采用用户明确点击“继续生成”的方式发起新请求。

### 第二轮追问：如果要支持真正的断线恢复，协议还需要什么？

**参考回答：**

可演进为生成任务与传输连接分离：创建任务返回 generationId，服务端保存按序号排列的事件；重连携带最后确认的序号，服务端重放之后的事件，客户端按任务和序号去重。创建任务还需要幂等键，避免重复启动模型。

只加一个 Last-Event-ID 请求头不够，服务端必须有事件存储、保留期限和恢复语义。项目现在的“继续生成”属于模型续写，不是原连接的事件恢复。

协议基础可参考 [MDN：使用服务端发送事件](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)。

## 02. 网络 chunk 为什么不能直接 JSON.parse？

### 主问题：流读取到一段 Uint8Array 后，你怎么得到完整事件？

**参考回答：**

我分三层处理：字节先通过流式 TextDecoder 解码成文本；文本进入行缓冲，保留未完整的行；SSE 解析器把多行 data 字段组合成事件，遇到事件结束空行再解析 JSON。

网络 chunk 没有业务边界，一次读取可能只有半个字符，也可能包含多个事件。因此不能把一次 reader.read() 的结果当作一个 JSON，也不能认为每个 chunk 就是一个模型 Token。

### 第一轮追问：中文、emoji 和 CRLF 刚好被切开怎么办？

**参考回答：**

TextDecoder 使用 `{ stream: true }` 保留未完整的 UTF-8 字节序列，结束时再调用一次 decode 完成解码。项目使用 fatal 模式，遇到非法字节时中断并保留已有回答，避免悄悄替换损坏内容。

行解析分别处理 LF、CR 和 CRLF。末尾只有 CR 时先等待下一段，确认后面是否有 LF，防止把一次换行错误地解析为两次换行。SSE 的多行 data 用换行连接，注释行忽略；只有事件组装完成才交给 JSON.parse。

### 第二轮追问：攻击者一直发送不换行的文本，会不会撑爆内存？

**参考回答：**

需要分别限制未完成行缓冲、单事件大小和整条回答大小。项目对事件相关缓冲设置约百万字符上限，对回答也设置长度上限，超限后取消消费并报告错误。

但应用层检查不是完整的入口防护：上游客户端可能先缓冲了一整行，单次网络读取也可能已经很大。可演进的服务还应设置请求体、读取缓冲、并发和超时限制。内存上限与解析 CPU 成本需要一起评估。

## 03. 如何判断流是真正完成，还是意外断开？

### 主问题：为什么 reader.read() 返回 done，不能直接显示“回答完成”？

**参考回答：**

reader 的 done 只说明响应体结束，不代表业务完成。服务可能崩溃，代理也可能断开连接。

项目定义了 meta、delta、done、error 四类事件：meta 提供模型和来源；delta 提供新增正文；done 明确宣布成功并返回用量；error 表示生成失败。没有收到业务 done 就发生 EOF，会显示中断并保留部分回答。上游模型也必须返回约定的完成标记，否则后端按中断处理。

### 第一轮追问：已经返回 HTTP 200，生成到一半报错，还能改成 500 吗？

**参考回答：**

响应头发送后不能再改 HTTP 状态码。项目把校验、资料检索等准备工作放在开始发送流之前，这些错误可以正常返回 4xx；开始流式响应后，业务异常通过 error 事件传递。

如果连 error 都没送到，客户端仍能用“缺少 done”识别异常。完整性判断因此不能只依赖 HTTP 200。

### 第二轮追问：服务端说自己逐段 yield，浏览器为什么还是最后一次性显示？

**参考回答：**

要逐层检查模型、后端、代理与浏览器。先确认上游确实增量返回，后端没有先调用 text/json 收完整响应，再检查反向代理、压缩和网关是否缓冲。项目返回 text/event-stream、Cache-Control 和 X-Accel-Buffering: no，但这些头不能强制所有网关按预期工作。

可在各层记录首个增量时间，用不缓冲的 HTTP 客户端对比直连后端和经过代理的结果。如果网络已经分段到达、页面仍不更新，再检查前端调度和主线程长任务。

## 04. “一帧渲染一次”具体是怎么实现的？

### 主问题：为什么不在每个 delta 回调里直接更新 Markdown？

**参考回答：**

网络到达频率与显示刷新频率不一致。同一帧可能收到很多片段，逐片段解析 Markdown、清洗 HTML 和更新 DOM 会重复处理用户看不到的中间状态。

项目把正文保存在非响应式缓冲区；append 只累积文本并尝试安排 rAF。存在待执行的回调就不再安排，回调读取最新完整快照，生成一次 HTML，再更新 Vue 状态。这里保证的是本缓冲器每帧最多调用一次正文渲染函数，不是承诺整页每帧只发生一次 DOM 更新。

### 第一轮追问：Vue 自己不是已经批处理更新了吗？nextTick 不够吗？

**参考回答：**

Vue 的批处理可以合并某些组件更新，但无法消除我在每次回调里显式执行的 Markdown 解析和 DOMPurify 调用。多个网络读取也可能跨越不同调度周期。

nextTick 用于等待 Vue 更新完成，适合随后读取滚动高度；它不负责按显示帧率节流。rAF 负责安排正文计算时机，两者解决不同问题。

### 第二轮追问：60 Hz 下是不是每 16.7 ms 运行一次？最后一段怎么保证显示？

**参考回答：**

rAF 不是固定周期定时器，回调时机受刷新率、主线程负载和页面可见性影响。项目的 finish 把最终状态并入同一个待执行回调，最后一次统一渲染并完成 Promise；卸载时 dispose 取消回调并解除等待。

隐藏页面的 rAF 可能暂停，因此当前 finish 可能等到页面重新可见才完成。可演进时应把“网络已完成”和“最终界面已提交”拆成不同状态，隐藏时保留数据，恢复可见后再渲染。不能为了追求立即完成，额外同步调用一遍正文渲染而破坏合并策略。

rAF 的调度与后台行为可参考 [MDN：requestAnimationFrame](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)。

## 05. 每帧最多一次，为什么长回答仍然可能卡顿？

### 主问题：这个方案有什么性能边界？

**参考回答：**

每帧合并只减少执行次数，没有降低一次工作的成本。项目每次处理完整正文，文本越长，Markdown 解析、清洗和 HTML 替换越贵。如果长度从 1 逐渐增长到 n，且频繁重新处理全文，在近似线性单次处理成本的前提下，累计工作量可能接近二次增长。

当前通过限制长度、流式阶段不高亮代码、缓存生成好的 HTML，控制常见回答的开销；它不等于实现了高性能增量 Markdown 引擎。

### 第一轮追问：如何判断瓶颈在解析还是浏览器布局？

**参考回答：**

分别测量解码和事件解析、Markdown 解析、DOMPurify、Vue 更新，再用浏览器 Performance 面板观察样式计算、布局与绘制。还要检查是否每次读取 scrollHeight 后又写 scrollTop，引发布局压力。

只在 rAF 内测函数耗时不够，因为 Vue 的 DOM 提交可能发生在随后调度中。应结合长任务、交互延迟、不同回答长度和真实设备分析。项目目前没有生产性能基准，不能把“理论上每帧一次”说成“实测始终 60 FPS”。

### 第二轮追问：如果要支持十万字回答，你会怎样升级？

**参考回答：**

可演进为按块存储和渲染：尽量固定已稳定的块，只重新处理不稳定尾部；长历史消息做虚拟化；将可移出的解析工作放到 Worker，并通过版本号丢弃过时结果。

难点是 Markdown 的后续内容可能改变前文解释，例如引用式链接定义、列表延续和表格识别，不能简单认为“遇到空行就永远稳定”。需要限制支持语法，或使用有依赖追踪能力的增量解析器。Worker 也不会消除主线程上的 DOM 提交成本，DOMPurify 的使用环境还需要单独设计。

## 06. 半个 Markdown 标签如何安全、稳定地显示？

### 主问题：流停在代码块或加粗标记中间，会不会把页面弄乱？

**参考回答：**

我始终保留原始 Markdown，把当前完整快照交给解析器生成 HTML，再清洗后替换展示内容，不直接追加 HTML 字符串。

对于项目支持的常见语法，未结束的围栏代码块会被解析为结构闭合的 HTML 代码块；不完整的强调、链接等根据当前语法先显示为文本，后续字符到达后重新解析。HTML 结构闭合与 Markdown 意图完整是两回事，不能因为 DOM 能展示就认定答案已完成。

### 第一轮追问：为什么不统计反引号或星号数量，缺几个就补几个？

**参考回答：**

这些字符可能出现在转义文本、行内代码、代码块、嵌套列表和普通内容中，简单计数无法确定语义。自动补符号还可能把临时修复写入原始正文，导致后续续写接不上。

项目不修改原文来猜测闭合，只生成可展示的当前快照。若产品需要隐藏半成品链接，应作为展示层规则实现，并明确展示文本与模型原文不同。

### 第二轮追问：每次替换 innerHTML 会损失什么？

**参考回答：**

可能影响文字选择、焦点、代码块滚动位置，也会重复创建节点。当前方案对中等长度回答比较直接，但不应宣称没有交互副作用。

可演进为稳定块组件、只替换尾部，并对关键交互元素保持稳定标识；代价是需要处理跨块语法依赖。项目采用容器事件代理处理引用点击，避免每次生成 HTML 后重新逐个绑定按钮事件，但这并不解决所有焦点和选择问题。

## 07. 模型输出为什么也必须防 XSS？

### 主问题：回答来自自己的后端，为什么还要清洗？

**参考回答：**

模型可能复述用户输入、知识库资料或外部网页中的攻击内容，因此生成文本仍是不可信输入。Markdown 解析也不自动等于 HTML 安全，Marked 明确要求调用方处理输出清洗，见 [Marked 官方说明](https://marked.js.org/)。

项目先把模型原始 HTML 转为文本；Markdown 生成 HTML 后，再用 DOMPurify 的明确标签和属性白名单清洗，最后交给 v-html。链接只允许 HTTP/HTTPS，Markdown 图片转为链接，避免默认加载任意外部图片。

### 第一轮追问：只删除 script 标签，或者只替换尖括号够不够？

**参考回答：**

不够。攻击还可以通过事件属性、危险 URL、SVG 等结构进入。单纯转义原始文本也不能替代对后续 Markdown 插件和自定义渲染器生成 HTML 的检查。

项目把 DOMPurify 放在 HTML 生成链路的最后，并对允许的属性逐项控制。清洗后再用不可信字符串拼接 HTML，会重新引入风险。安全策略与限制配置可参考 [DOMPurify 官方说明](https://github.com/cure53/DOMPurify)。

### 第二轮追问：通过 XSS 测试，是不是就能说这个问答系统安全了？

**参考回答：**

不能。XSS 防护处理的是浏览器执行风险，不等于解决提示词注入、答案事实错误、来源伪造和越权访问。清洗依赖的版本与配置也需要持续维护。

当前是本地单工作空间实现。续写会接收客户端提交的来源快照；多用户场景应由服务端保存可信快照，以受权限校验的会话 ID 引用，不能把客户端回传的资料当作权威来源。还可以增加 CSP 等纵深措施，但不能用 CSP 替代清洗。

## 08. Chunk 引用为什么容易在流式改造中丢失？

### 主问题：如何保留来源引用样式和点击定位？

**参考回答：**

项目在 Markdown 的文本渲染阶段识别完整的 Chunk/Web 引用，生成应用控制的按钮，保留颜色类名和 data-chunk-ref、data-web-ref。容器收到点击后，展开来源面板，定位并高亮对应片段。

引用样式不仅依赖 CSS，还依赖自定义渲染器和清洗白名单。清洗器如果删除 data 属性，按钮看起来还在，但定位会失效。因此测试既检查按钮样式类，也检查引用属性和目标编号。

### 第一轮追问：为什么不能对原始 Markdown 全局 replace 引用？

**参考回答：**

代码示例里也可能出现“（Chunk 8）”。全局替换会把本应按字面展示的代码变成引用 HTML，还可能破坏 Markdown 语法。

项目在文本渲染阶段处理引用，让代码块和行内代码保持字面内容。流式阶段只有完整引用才转成按钮，半个引用先作为文本显示。原始模型 HTML 被转义，也不能直接伪造应用按钮。

### 第二轮追问：模型写了一个不存在的 Chunk 编号，按钮能证明引用真实吗？

**参考回答：**

不能。当前点击逻辑会检查本次返回的来源是否存在目标，没有就提示未找到来源；按钮本身不能证明答案受到资料支持。

可演进为服务端返回结构化引用，并把引用 ID 与本次检索快照绑定。即便编号存在，也还需要评估对应片段是否支持回答中的主张。引用可点击、来源存在和事实有依据，是三种不同层次的验证。

## 09. “停止生成”应该停止哪一层？

### 主问题：点击停止，为什么不能只是把 loading 改成 false？

**参考回答：**

那样只改变界面，网络和模型可能仍在运行。项目通过 AbortController 取消 fetch，并取消 reader；后端感知断开或取消后关闭上游异步流，在 finally 中记录部分结果。

界面保留已收到的原始正文，最后做一次合并后的安全渲染，状态改为 stopped。异常中断使用 error 状态，成功则必须收到业务 done。停止和失败都不清空已有回答。

### 第一轮追问：用户刚点停止，下一条 delta 已经到达，会不会串状态？

**参考回答：**

取消存在竞争，不能假设点击之后所有层都瞬间静止。已经进入缓冲区的文本允许保留；取消后检查 signal，停止继续消费。当前同一时刻只允许一个生成任务，继续按钮也会等 busy 结束后才可操作。

流的 controller 和 buffer 归属当前请求，清理时比较实例，避免旧请求清理新请求资源。组件卸载还会 abort 和 dispose，取消待执行帧。若未来允许并发回答，应该进一步用 turnId、generationId 隔离每个请求的状态和回调。

### 第二轮追问：取消 fetch 能保证模型立即停止计费吗？

**参考回答：**

不能。后端关闭连接是在释放本项目持有的资源，供应商何时停止计算取决于其服务行为。取消之前产生但尚未传回的 Token，也可能已被计费。

因此项目只记录供应商实际报告的用量，拿不到就保留未知，不能把取消等同于零消耗。若供应商支持独立任务取消和用量查询，可进一步做取消确认与后续对账。

## 10. “继续生成”是真正恢复原来的模型流吗？

### 主问题：你是怎样接着已停止的回答继续输出的？

**参考回答：**

原请求已经取消，当前实现会发起新的模型请求。它沿用该条回答保存的模型、原问题和来源快照，把已生成正文放入 assistant 消息，再要求模型只输出新增内容，并延续原来的 Markdown 和引用编号。

前端先把已有原始 Markdown 放进本次缓冲区，再追加新 delta，更新同一条回答；不会把之前生成的 HTML 作为模型上下文。未拿到来源就停止时，则重新执行正常准备流程。

### 第一轮追问：模型重复开头，或者又输出一遍代码围栏怎么办？

**参考回答：**

当前通过续写提示约束，但不能保证模型逐字无缝续接，项目没有实现强制去重。每次续写时完整 Markdown 会重新解析；它能处理正确续接的语法，不能自动纠正所有模型重复。

可演进为先缓冲少量续写前缀，检测与旧正文后缀的精确重叠，明确匹配后再去重。但重复代码和列表项可能本来就是合法内容，所以不能用激进的模糊去重。要求严格恢复时，应采用服务端缓存原生成任务的方案。

### 第二轮追问：连续续写很多次，上下文和费用会怎样变化？

**参考回答：**

每次都发送累计正文，会增加输入 Token，并可能超过模型上下文窗口。字符上限不能替代模型 Token 预算检查；当前长度限制只是保护应用资源。

可演进时按模型预算预留输出空间，超限后明确提示或使用摘要加尾部内容。摘要可能丢失精确信息，也不适合直接替代未闭合代码块的上下文，因此要保留必要语法状态和资料依据。无论是否使用缓存，每次续写都应作为新的实际调用记录用量。

## 11. 流式回答的 Token 统计为什么不能按文字长度算？

### 主问题：流还没结束时，你怎么统计消耗？

**参考回答：**

模型增量是传输内容，不是统一的 Token 单位；语言、编码和模型 tokenizer 都会影响计数。项目请求上游报告 usage，可能在最后收到仅包含用量、不包含正文的事件，再由后端保存实际报告值。

中断前收到部分用量就保留，没有收到就标为未知。不能把缺失值补成 0，也不能把字符长度当作真实账单。继续生成新增一条调用记录，监控汇总这些记录。

### 第一轮追问：前端没收到 done，但数据库已经记录成功，会不会矛盾？

**参考回答：**

不一定。上游可能已经完成，后端也保存成功记录，但 done 在传给浏览器的途中丢失。当前日志主要反映模型调用是否完成，界面状态反映浏览器是否确认收到完成事件，这两个观察点不同。

可演进为分别保存模型完成、结果持久化、客户端确认等状态，界面通过 generationId 查询最终结果，避免为了确认结果又重新生成一次。当前项目没有完整的结果确认协议。

### 第二轮追问：finally 写一次日志，就能说做到 exactly-once 吗？

**参考回答：**

不能。finally 有助于在正常结束、异常和取消路径统一记账，但无法覆盖进程崩溃、事务提交后响应丢失或跨请求重试。项目测试证明的是这些受控路径下一次请求只记一条记录。

更强的保证需要调用 ID、数据库唯一约束、幂等更新和可恢复任务状态；外部供应商调用还需要相应的幂等或查询能力。不能把单个函数只执行一次写入，等同于端到端 exactly-once。

## 12. 你如何证明流式渲染真的可靠？

### 主问题：测试流式功能时，只看页面文字逐渐出现够不够？

**参考回答：**

不够。我会按协议、调度、渲染和业务生命周期分层测试。项目已有前端 7 项、后端 15 项测试，其中包含本轮流式相关覆盖；后端总数也包含知识库原有回归测试。

关键用例包括：按字节切分中文和 emoji、跨段 CRLF、多行事件、缺少 done、取消等待中的读取、同帧多个 delta 只渲染一次、结束刷新、代码块未闭合、引用在代码中保持字面值、攻击文本的每个前缀、部分用量记录，以及连续续写携带累计正文。

### 第一轮追问：为什么要测试恶意文本的每一个前缀？

**参考回答：**

流式渲染展示的不只是最终字符串，还包含很多中间状态。最终安全的输入在不完整阶段可能经过不同的 Markdown 解析路径，反过来也一样。

项目对若干 HTML、危险链接和伪造引用样本逐字符截取，每个前缀都经过真实渲染和清洗，再检查 DOM 中是否出现危险元素、事件属性或不允许的链接。它验证特定攻击集和配置，不能当作形式化安全证明。

### 第二轮追问：单元测试通过后，生产前还缺什么？

**参考回答：**

还需要真实浏览器与部署链路测试：后台标签页恢复、连续停止续写、卸载取消、文字选择、滚动跟随、移动端长回答、真实代理是否缓冲，以及供应商流格式差异。可以增加随机分包的属性测试，要求同一事件流在不同字节切分下解析结果一致。

当前已通过构建与自动化测试，但尚未完成浏览器实测，也没有真实供应商的完整性能压测。因此面试中可以说明已验证的分层能力，不能编造生产 QPS、首字延迟或掉帧率。

## 高频误区速查

| 容易说错的话 | 更准确的表达 |
| --- | --- |
| SSE 只能 GET | 原生 EventSource 有接口限制；本项目用 POST fetch 消费 SSE 格式 |
| 一个 chunk 就是一个 Token | 网络分块、事件分块与模型 Token 不是同一边界 |
| HTTP 200 就生成成功 | 还要收到业务完成事件 |
| rAF 保证固定 60 FPS | 它提供绘制前调度，实际帧率和回调时机受环境影响 |
| Vue 批处理就不需要合并解析 | 显式执行的 Markdown 解析不会自动被 Vue 消除 |
| Markdown 自动防 XSS | 解析和安全清洗是两个步骤 |
| 自动补几个反引号就行 | 语法上下文复杂，不能污染原始正文 |
| 有引用按钮就代表回答可信 | 还需验证来源存在、权限合法且支持对应主张 |
| 停止就是零费用 | 取消不代表供应商未计算或未计费 |
| 继续生成就是断点恢复 | 当前是携带上下文的新模型调用 |
| finally 记日志就是 exactly-once | 它不覆盖崩溃、重试和外部调用一致性 |

## 建议练习方式

第一遍只回答主问题，每题控制在一分钟左右；第二遍加入第一轮追问，解释具体实现；第三遍回答第二轮追问，主动说明方案边界和演进条件。回答时始终区分“项目已经做了什么”“测试验证了什么”和“规模扩大后准备怎样改”。
